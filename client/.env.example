# Optional: public URL of your Express API (no trailing slash).
# Leave unset locally — `npm run dev` proxies /api to port 8788.
# Required for production static hosts (e.g. Vercel): set matching value in CI / dashboard env.
#
# VITE_API_BASE_URL=https://your-backend.onrender.com
